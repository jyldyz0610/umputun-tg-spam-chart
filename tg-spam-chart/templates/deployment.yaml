apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tg-spam.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tg-spam.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "tg-spam.fullname" . }}
  template:
    metadata:
      labels:
        app: {{ include "tg-spam.fullname" . }}
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000

      initContainers:
        - name: init-permissions
          image: busybox
          command: ["sh", "-c", "chown -R 1000:1000 /srv/data"]
          securityContext:
            runAsUser: 0
            runAsGroup: 0
          volumeMounts:
            - name: tg-data
              mountPath: {{ .Values.tgspam.volumeMountPath }}

      containers:
        - name: tg-spam
          image: {{ .Values.tgspam.image }}
          env:
            - name: TELEGRAM_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secretName | default "tg-spam-bots-secret" }}
                  key: {{ .Values.tgspam.env.TELEGRAM_TOKEN_KEY }}
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secretName | default "tg-spam-bots-secret" }}
                  key: OPENAI_API_KEY
            {{- range $key, $value := .Values.tgspam.env }}
            {{- if and (ne $key "TELEGRAM_TOKEN_KEY") (ne $key "OPENAI_API_KEY") }}
            - name: {{ $key }}
              value: "{{ $value }}"
            {{- end }}
            {{- end }}
          args:
{{ toYaml .Values.tgspam.args | indent 12 }}
          volumeMounts:
            - name: tg-data
              mountPath: {{ .Values.tgspam.volumeMountPath }}

      volumes:
        - name: tg-data
          persistentVolumeClaim:
            claimName: {{ include "tg-spam.fullname" . }}-tg-data

